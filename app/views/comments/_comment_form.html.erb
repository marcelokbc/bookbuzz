<div class="space y-4 bg-gray-100 p-4 rounded-lg mt-2">
  <% if action_name == 'edit' %>
    <h2 class="text-gray-700 font-semibold mb-4">Editar Comentário</h2>
  <% else %>
    <p class="text-gray-700 mb-2"> Deixe seu comentário </p>
  <% end %>
  <%= form_for [@book, @comment] do |f| %>
    <div>
      <%= f.text_area :content %>
    </div>
    <div>
    <% if action_name == 'edit' %>
      <%= f.submit 'Editar Comentário', class: 'bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded' %>
    <% else %>
      <%= f.submit 'Editar Comentário', class: 'bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded' %>
    <% end %>
      <%= link_to 'Cancelar', book_path(@book), class: 'bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded' %>
    </div>
  <% end %>
</div>